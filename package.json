{
  "name": "realityshift",
  "private": true,
  "version": "0.1.0",
  "description": "A persistent multi-agent world simulation where AI agents run every country based on real history; fork the world and take over any country while the rest react.",
  "scripts": {
    "dev": "npm --prefix apps/web run dev",
    "build": "npm --prefix apps/web run build",
    "lint": "npm --prefix apps/web run lint",
    "typecheck": "npm --prefix apps/web run typecheck",
    "preview": "npm --prefix apps/web run preview",
    "worker:dev": "npm --prefix apps/worker run dev",
    "worker:deploy": "npm --prefix apps/worker run deploy",
    "worker:typecheck": "npm --prefix apps/worker run typecheck",
    "install:all": "npm --prefix apps/web install && npm --prefix apps/worker install"
  }
}
